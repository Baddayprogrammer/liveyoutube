[build]
  # Invece di vuoto, diamo un comando che non fa nulla ma dice "tutto ok"
  command = "echo 'Build skipped'"
  
  # Usiamo il punto per dire "cartella corrente" invece dello slash
  publish = "." 
  
  # Confermiamo dove sono le funzioni
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
